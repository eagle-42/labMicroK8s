apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - pod.yaml

namespace: labs

labels:
  - includeSelectors: false
    pairs:
      module: 01-pods
      managed-by: kustomize
