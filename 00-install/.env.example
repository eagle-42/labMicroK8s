# K8SLab VM Configuration
# Copy this to .env and update with your settings

# SSH Connection (uses your ~/.ssh/config host alias)
VM_HOST=vm-ubuntu

# Or specify full SSH details if not using SSH config:
# VM_HOST=your-vm-ip-or-hostname
# VM_USER=your-username
# VM_SSH_KEY=~/.ssh/your_key
