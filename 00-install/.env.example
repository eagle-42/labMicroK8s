# Copy to .env. VM_HOST is the ~/.ssh/config alias of the machine running MicroK8s.
VM_HOST=vm-ubuntu

# How your workstation reaches THIS machine over ssh, for `make dashboard`.
# Left empty it falls back to this machine's hostname, which your workstation
# may well not resolve - set it to the alias in your own ~/.ssh/config.
GATEWAY=
